{
  "name": "je-suis-beatz",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start-proxy": "node proxy/server.js",
    "start-static": "npx http-server -p 8000 -c-1",
    "start-all": "npx concurrently \"npx http-server -p 8000 -c-1\" \"npm run start-proxy\"",
    "test:e2e": "npx playwright test",
    "test": "echo \"No unit tests defined. Run \"npm run test:e2e\" for E2E.\" && exit 0"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "@genkit-ai/ai": "^1.37.0",
    "@genkit-ai/googleai": "^1.28.0",
    "express": "^4.18.2",
    "genkit": "^1.37.0",
    "jimp": "^1.6.1",
    "node-fetch": "^2.6.7"
  }
}
